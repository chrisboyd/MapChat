1. Run this in the command line to generate the project

npx @googlemaps/js-samples init map-simple DESTINATION_FOLDER

2. Replace the contents of the src folder with the files provided

3. Add your API key to the link in the HTML file

4. Run the following command if you need to start the node runtime environment

5. Code should run in localhost:8080

npm run dev
